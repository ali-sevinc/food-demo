export type FoodType = {
  id: string;
  description: string;
  image: string;
  name: string;
  price: string;
};
export type CartType = {
  id: string;
  name: string;
  price: number;
  quantity: number;
};
